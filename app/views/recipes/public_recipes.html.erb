<%= render partial: 'shared/header' %>
<div class="flex flex-col gap-5 w-full md:w-[60%] mx-auto">
  <h1 class="md:text-4xl text-2xl font-extrabold mx-auto">Public Recipes</h1>
  <% @public_recipes.each do |public_recipe| %>
   <%= render partial: 'shared/public_recipe_card', locals: { public_recipe: public_recipe } %>
  <% end %>
</div>